services:
  - type: web
    name: pos-parent-agent
    env: python
    plan: free
    region: singapore
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python parent_agent.py"
    envVars:
      - key: GROQ_API_KEY
        sync: false
